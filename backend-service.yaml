apiVersion: v1
kind: Service
metadata:
  name: scheduling-service
spec:
  selector:
    app: scheduling-microservice
  ports:
    - protocol: TCP
      port: 9002
      targetPort: 9002
  type: LoadBalancer
